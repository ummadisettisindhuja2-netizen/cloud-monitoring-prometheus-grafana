global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: "monitoring-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "app:5000"
